import React from "react";
import Section from "./Main/Section";

const PairingList = () => {
    return (
        <React.Fragment>
            <Section heading={'pairing header'} paragraph={'pairing description'} />
            <Section heading={'pairing header'} paragraph={'pairing description'} />
            <Section heading={'pairing header'} paragraph={'pairing description'} />
        </React.Fragment>
    )
}

export default PairingList;